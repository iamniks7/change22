import React from 'react';
import { ZoomIn, ZoomOut } from 'lucide-react';

export function RotateInstructions() {
  return (
    <div className="mt-3">
      <p className="text-center text-sm text-gray-500">
        <strong>Pro tip: </strong>Use the zoom in or zoom out buttons to adjust the image view while rotating:
      </p>
    </div>
  );
}